public class WordlePlayer
{
    //input is the results of the previous guess
    //or null if this is the first guess
    //0 is letter not in word
    //1 is letter in word, but not at correct spot
    //2 is letter in word and at correct spot
    //returns a string which is the current guess
    public String getGuess(int [] previousResults, int guessesRemaining)
    {
        return "hello";
    }
}